name: hycom-analysis-env
channels:
  - conda-forge
dependencies:
  - python=3.11
  - xesmf          # conda-forge only; no PyPI wheels
  - jupyterlab
  - ipykernel
  - pip
  - pip:
      - -e "..[dev]"   # installs xhycom + all other dependencies via pyproject.toml
